#include <string>
class PhoneBook {
public:
    void add(const std::string& name, const std::string& number) {
        // Add the name and number to the phone book
    }

    std::string get(const std::string& name) {
        // Return the number associated with the name
    }

    void remove(const std::string& name) {
        // Remove the name and number from the phone book
    }
};